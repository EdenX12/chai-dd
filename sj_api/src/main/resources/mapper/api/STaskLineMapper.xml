<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.api.mapper.STaskLineMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cc.mrbird.febs.api.entity.STaskLine">
        <result column="id" property="id" />
        <result column="product_id" property="productId" />
        <result column="total_task" property="totalTask" />
        <result column="received_task" property="receivedTask" />
        <result column="line_status" property="lineStatus" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, product_id, total_task, received_task, line_status, create_time, updateTime
    </sql>
    <select id="queryCurrentTaskLine" resultType="java.lang.String">
      select id  from s_task_line where   line_order=(
      SELECT  MIN(line_order) AS minOrder FROM s_task_line WHERE line_status = 0 AND product_id=#{productId});
    </select>

    <select id="queryMinLineOrder" resultType="java.lang.Integer">
        SELECT  min(line_order)   FROM s_task_line WHERE  product_id=#{productId}  WHERE line_status = 0
    </select>


</mapper>
