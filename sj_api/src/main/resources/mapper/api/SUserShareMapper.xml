<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.api.mapper.SUserShareMapper">
    <select id="getCurrentShareId" resultType="java.lang.String">
    select share_id  from s_user_browser where user_id=#{userId} and product_id =#{productId} ORDER BY create_time desc limit 1
    </select>

</mapper>
