<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.api.mapper.SUserBonusLogMapper">

    <select id="findUserBonusRewardSum" resultType="java.math.BigDecimal">
        SELECT SUM(bonus_amount) bonusAmount
          FROM s_user_bonus_log
         WHERE user_id=#{userId}
           AND bonus_type IN (3, 4)
           AND status IN (0, 1)
    </select>

    <select id="findUserBonusRewardTodaySum" resultType="java.math.BigDecimal">
        SELECT SUM(bonus_amount) bonusAmount
          FROM s_user_bonus_log
         WHERE user_id=#{userId}
           AND bonus_type IN (3, 4)
           AND status IN (0, 1)
           AND to_days(create_time) = to_days(now())
    </select>
</mapper>
