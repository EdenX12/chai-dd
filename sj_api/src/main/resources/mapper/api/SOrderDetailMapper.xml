<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.api.mapper.SOrderDetailMapper">


    <select id="queryProductByOrder" resultType="java.util.Map">
        select product_id productId,order_product_id orderProductId  from s_order_product where order_detail_id in(
        select id  from s_order_detail where order_id=#{orderId})
    </select>

</mapper>
